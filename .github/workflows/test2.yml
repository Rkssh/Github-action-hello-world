name: Github test action plan

on: [push]

jobs:
  Test_Job2:
    name: testing workflows
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world this is running a ${{ runner.os }} server in GitHub"
